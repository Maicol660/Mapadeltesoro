import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Juego6Page } from './juego6.page';

describe('Juego6Page', () => {
  let component: Juego6Page;
  let fixture: ComponentFixture<Juego6Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Juego6Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
